# Expo public config (committed as a template; copy to app/.env for local overrides).
# Only EXPO_PUBLIC_*-prefixed vars are exposed to the client bundle.
# Points the app at the local LlmWiki.Api (http profile = port 5080).
EXPO_PUBLIC_API_BASE_URL=http://localhost:5080
